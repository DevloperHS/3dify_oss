# Postgres (docker compose up -d)
DATABASE_URL=postgres://maker:maker@localhost:5432/maker
TEST_DATABASE_URL=postgres://maker:maker@localhost:5432/maker_test

# Redis (docker compose up -d)
REDIS_URL=redis://localhost:6379

# Better Auth — generate a secret with: npx @better-auth/cli secret
BETTER_AUTH_SECRET=replace-me
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth — create at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=replace-me
GOOGLE_CLIENT_SECRET=replace-me
