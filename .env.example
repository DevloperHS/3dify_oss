# Postgres (docker compose up -d)
DATABASE_URL=postgres://maker:maker@localhost:5432/maker
TEST_DATABASE_URL=postgres://maker:maker@localhost:5432/maker_test

# Redis (docker compose up -d)
REDIS_URL=redis://localhost:6379

# Better Auth — generate a secret with: npx @better-auth/cli secret
BETTER_AUTH_SECRET=replace-me
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth — create at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=replace-me
GOOGLE_CLIENT_SECRET=replace-me

# Cloudinary (Source Image storage) — dashboard: https://console.cloudinary.com
CLOUDINARY_CLOUD_NAME=replace-me
CLOUDINARY_API_KEY=replace-me
CLOUDINARY_API_SECRET=replace-me

# Cloudflare R2 (generated GLB Asset storage) — create an API token with
# Object Read & Write on the bucket: https://dash.cloudflare.com → R2
R2_ACCOUNT_ID=replace-me
R2_ACCESS_KEY_ID=replace-me
R2_SECRET_ACCESS_KEY=replace-me
R2_BUCKET=maker-assets
